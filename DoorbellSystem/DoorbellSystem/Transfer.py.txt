import paho.mqtt.publish as publish
import serial
import time

ser = serial.Serial('dev/ttyS0', 9600)

while True:
	line = ser.readline()
	publish.single("/assignment/distance", line, hostname="172.20.10.10")